---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ if .Description }}{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}{{ end }}
---

# {{.Name}} ({{.Type}})

{{ if .Description }}{{ .Description | trimspace }}{{ end }}

{{ .SchemaMarkdown | trimspace }}

{{ if eq .Name "pulsar_cluster" -}}
## Import

```shell
terraform import pulsar_cluster.example standalone
```
{{ else if eq .Name "pulsar_tenant" -}}
## Import

```shell
terraform import pulsar_tenant.example public
```
{{ else if eq .Name "pulsar_namespace" -}}
## Import

```shell
terraform import pulsar_namespace.example public/default
```

Target namespace must exist. Import reads namespace listing, topic and subscription dispatch rates, persistence, and backlog quotas; credentials need read access to each. Other optional blocks are not hydrated.

## Upgrade

v0.11 and v0.12 release-candidate state upgrades automatically; re-import is not required. With `-refresh=false`, migration performs no Pulsar API calls or policy writes.
{{ else if eq .Name "pulsar_topic" -}}
## Import

```shell
terraform import pulsar_topic.example persistent://public/default/example
```
{{ else if eq .Name "pulsar_function" -}}
## Import

```shell
terraform import pulsar_function.example public/default/example
```
{{ else if eq .Name "pulsar_source" -}}
## Import

```shell
terraform import pulsar_source.example public/default/example
```
{{ else if eq .Name "pulsar_sink" -}}
## Import

```shell
terraform import pulsar_sink.example public/default/example
```
{{ else if eq .Name "pulsar_permission_grant" -}}
## Import

The import ID uses the same format as the resource ID: `{namespace}/{role}` for namespace grants or `{topic}/{role}` for topic grants. The role is the segment after the last `/`, so roles containing `/` cannot be imported.

```shell
terraform import pulsar_permission_grant.example tenant/namespace/role1
```

```shell
terraform import pulsar_permission_grant.example persistent://tenant/namespace/topic1/role1
```
{{ else if eq .Name "pulsar_subscription" -}}
## Import

```shell
terraform import pulsar_subscription.example persistent://public/default/example@example-subscription
```
{{ else if eq .Name "pulsar_package" -}}
## Import

```shell
terraform import pulsar_package.example function://public/default/example@1.0.0
```
{{ end -}}
